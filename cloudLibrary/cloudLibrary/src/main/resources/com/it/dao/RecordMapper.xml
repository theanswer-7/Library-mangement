<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.it.dao.RecordMapper">

    <insert id="addRecord" parameterType="record">
        insert into record (record_bookname, record_bookisbn, record_borrower, record_borrowtime, record_remandtime)
        values (#{bookname}, #{bookisbn}, #{borrower}, #{borrowTime}, #{remandTime})
    </insert>

</mapper>
